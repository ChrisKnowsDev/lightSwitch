const checkBox = document.querySelector('input');
const html = document.querySelector('html');

function update(bgColor, txtColor) {
  document.body.backgroundColor = bgColor;
  document.body.color = txtColor;
}